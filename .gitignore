# ── Secrets & Environment ──
.env
.env.local
.env.*.local

# ── Real config (contains real keys, tokens, paths) ──
# The template config at config/config.json is committed (sanitized).
# The real config lives in data/.tingly-box/ and is NEVER committed.
data/

# ── OS files ──
.DS_Store
.DS_Store?

# ── Real config (contains your actual keys, tokens, paths, routing rules) ──
config/
# ── Real data and secrets (never commit) ──
# The real config lives in data/.tingly-box/ and is NEVER committed.
data/

# ── Docker compose override (personal env vars + volumes) ──
docker-compose.override.yml
docker-compose.override.*.yml

# ── Docker build artifacts ──
.docker/